spring:
  profiles:
    active: dev #默认为开发环境
mybatis:
  mapper-locations:
    - classpath:mapper/**/*.xml
pagehelper:
  helper-dialect: mysql
  reasonable: true
  support-methods-arguments: true
  params: count=countSql
